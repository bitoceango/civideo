{
  "name": "worker",
  "version": "1.0.0",
  "description": "child-video Cloudflare Worker 后端：激活/媒体网关/进度/家长规则",
  "main": "src/index.js",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute child-video-db --remote --file=./schema.sql",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "wrangler": "^4.100.0"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  }
}
